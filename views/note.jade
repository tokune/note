.content
  .title
    input(type="text", ng-model="current_title.value")
  .note(contenteditable="true") 
form.btns(ng-submit="submit()")
  input.btn.btn-primary(type="submit", value="Save")
  a(href='#/')
    button.discard.btn.btn-danger Discard
